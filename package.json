{
  "name": "tfsf-igfb-engagement-extension",
  "version": "0.1.0",
  "private": true,
  "description": "TITAN FORGE SF IG/FB Engagement Chrome Extension (MV3, TypeScript)",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc -p tsconfig.json && node scripts/copy-static.mjs",
    "dev": "npm run build -- --watch",
    "test": "npm run build && node --test tests/buildPacket.test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.16",
    "typescript": "^5.6.3"
  }
}
